namespace Common
{
    public enum PurchaseResult
    {
        Fulfilled,
        PartiallyFullfilled,
        Unfulfilled,
        Error
    }

    public enum SalesResult
    {
        Fulfilled,
        Error
    }
}
