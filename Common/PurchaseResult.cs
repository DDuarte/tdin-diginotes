namespace Common
{
    public enum PurchaseResult
    {
        Fulfilled,
        PartiallyFullfilled,
        Error
    }

    public enum SalesResult
    {
        Fulfilled,
        Error
    }
}
